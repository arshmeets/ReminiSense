name = "reminisense"
version = "0.1.0"
description = "ReminiSense — a memory prosthetic for people with early-stage Alzheimer's. Multi-agent Jac graph: recognition whispers, growing encounter memory, deterministic double-dose prevention."
entry-point = "backend/main.jac"
kind = "web-app"

[byllm]
[byllm.model]
default_model = "${BYLLM_MODEL:-claude-sonnet-4-6}"
api_key = "${ANTHROPIC_API_KEY:-}"
