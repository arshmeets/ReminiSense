[project]
name = "recall"
version = "0.1.0"
description = "Recall — an AI networking memory for founders and investors. Jac relationship graph, agent walkers, by llm() extraction."
entry-point = "backend/app.jac"
kind = "service"
jac-version = "==0.34.7"

[byllm.model]
default_model = "claude-sonnet-4-6"
api_key = "${ANTHROPIC_API_KEY}"
